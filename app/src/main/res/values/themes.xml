<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.EcoTrack" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.EcoTrack.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_ecotrack_splash</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.EcoTrack</item>
    </style>

</resources>
